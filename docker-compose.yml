services:
  web:
    build: .
    container_name: goodmorningtech_web
    environment:
      - FLASK_APP=index.py
      - FLASK_ENV=production
    volumes:
      - .:/app
    expose:
      - 5000

  nginx:
    image: ./nginx
    container_name: goodmorningtech_nginx
    ports:
      - 1337:80
    depends_on:
      - web